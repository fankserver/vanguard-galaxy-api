# Travel observation

Since 0.1.9 the real adapter is implemented and installed: `TravelNativeAdapter` reduces source-grounded facts, `TravelPatches` installs native hooks, and `ModApi.Travel`/`ModApi.Station` are exposed only when the group binds. Every travel phase has now been driven in the owner's protected sandbox runs (see the evidence matrix below), so the matrix cells below are CONTROLLED NATIVE evidence rather than host-only claims. `RuntimeQualified` stays **false**: controlled native receipts are evidence for their own case identities, not full owner acceptance of the shipped API. Issue #12 is still open at the time of writing; this document records the evidence, and closure is the owner's decision.

## Adapter contract

- Each hop is a leg requested with its REAL waypoint: `SetRouteToPOI` requests `waypoints[0]` when it is an actual in-system target (never the final `targetPoi`); a cross-system waypoint is a gate/wormhole handoff owned by the jump iterator. A new `SetRouteToPOI` (a genuine new route) supersedes any pending leg with a truthful `Cancelled` + fresh `Request`; `TravelToNextWaypoint` (the same-leg continuation) never replaces a pending leg. Source-faithful order is preserved: `SetRouteToPOI` calls `CancelTravel()` before the route postfix requests the new leg.
- Direct gate/wormhole hops never need same-origin leg reuse: the jump iterator requests its leg fresh (the current gate is never in the route list). A lingering stale pending leg (re-route edge) is truthfully superseded (`Cancelled` + fresh `Request`), never relabelled.
- Departure is a verified transport boundary, never a method return and never merely `Current == null`/`InCurrentSystem`. A LOADED origin (dwell end) departs only on the verified `UnloadCurrentScene` origin->null manager transition, so an early warp cancel or hold-position at the origin yields `Cancelled(origin)` with no fabricated `Departed`. An ALREADY-UNKNOWN origin (empty-origin/re-route where `UnloadCurrentScene` is a NOOP) departs on the actual transport start — `TravelInSystem()`'s first step, which runs only after departure preparation and is robust to a mid-warp `CancelTravel`. Jump-step origin changes also depart. `TravelActive()` is intentionally not used because it includes preparation. A zero-distance hop that never warps emits no `Departed` and its leg is superseded by the next route (no invented physical travel).
- In-system arrival is observed through the `SpaceshipHasArrived` hierarchy with arrival scopes; jumpgate/wormhole arrival is observed by advancing owned nested iterators (`TravelJumpObserver`) and detecting destination manager readiness, captured before `TravelToNextWaypoint` can start the next leg. Nested child yields (Unity `WaitUntil`/`CustomYieldInstruction`) carry no lifecycle callbacks: only the root jump iterator terminal/disposal is cancellation, and a completed child never cancels a pending leg.
- `actualFinalRouteCompleted` is emitted once, from the verified `TravelToNextWaypoint` boundary (no remaining waypoints and `TravelActive()==false`), attributed to the leg recorded by the last arrival. Legs started by `TravelToNextWaypoint` (the in-system legs after a jump or in a fast-lane chain) are requested there, so they produce their own Arrived and can close the route.
- Departure is the bounded rule above: a loaded origin departs on the verified origin->null `UnloadCurrentScene` transition (NOOPs ignored); an already-unknown origin departs on the actual `TravelInSystem` transport-start boundary.
- The requested destination of a jump is built from the `JumpGate`'s RAW `targetSystemGuid`/`targetPoiGuid` (or the wormhole waypoint target), with no world/name lookup, so a nominal target absent from the current (tutorial/sandbox) map is still a valid request; there is no lazy fallback that could misread a rewritten current POI as the gate. The actual destination (including tutorial rewrites) is preserved on the arrived fact, and requested/origin are retained on the leg.
- Station facts are distinct from travel legs: physical dock/undocking/leaving are observed ONLY from native `Dock`/`Undock`/`EmergencyUndock` coroutine boundaries. `DockQuick` is used by the same-size restore/relink path and is never a physical transition, so it is not hooked at all. A real `Dock()` completion is eligible only when it belongs to a genuine native docking REQUEST: the intent is captured at the ACTUAL assignment (`DockingOption.AssignSpaceshipForDocking`) that happens inside a `SpacestationExteriorManager.CheckForDocking` scope (the arrival auto-dock, the HUD dock button and idle autopilot all route through it), pins an immutable session/player/ship/option, and is retained until the `Dock()` coroutine completes. Every other assignment callsite is a restore/relink/NPC/dungeon path (`InitializePoi(init: true)`, `GameplayManager.ReinitPlayerSpaceshipRoutine`, `SpaceShip.RelinkDockedShipToStation`, `SpaceStationActions`, `DungeonOperation`) and emits nothing, even the different-docking-size re-init that takes a real `Dock()` coroutine, and even when no interior exists. The interior scene/instance is NEVER a discriminator: on arrival `CheckForDocking` only ASSIGNS (the ship is still at the landing position) and the same synchronous call continues into `CheckForSpaceStationEnter`, which enters the interior for any previously visited station, while the actual `Dock()` coroutine is created frames later by `DockingOption.Update`'s approach — so a factory-time scene check would reject every genuine return dock. Assignment itself emits nothing; only a successfully completed `Dock()` root with the ship in physical `Docked` state does, and the intent is consumed there so a request yields exactly one fact (native `PerformDocking` requires `CanDock()` and sets `Docking` before starting the coroutine, so one assignment normally starts one `Dock()`; consuming the intent is a defensive guard, not a claim of native concurrency). An undock voids a pending intent, a superseded/restore assignment clears it, and a stale option/player/session can never consume another request's intent. Undocking/Leaving require the captured ship's `DockingState` and need no request intent (a physical exit is always real), and the station is read from the player's actual location. Dock/undock ownership pins an immutable session+player at FACTORY/PATCH time; at the first actual step it verifies that ownership is still current and only then captures the exact ship. A session/player replacement before or mid-coroutine therefore produces ZERO observer facts in the new session (never adopting it), and a factory created before the player is ready yields no manufactured session. Genuine current-ownership binding/reflection failures surface through `Guard` and are logged (never swallowed); a stale owner returns null without faulting a replacement. Interior readiness is an attributed nonthrowing `Awake`+`Start` with the exact live instance, station, player and session (Finalizer-only Awake attribution); no ordering is promised between `InteriorReady` and `DockedPhysical`, because the interior is opened by `CheckForSpaceStationEnter` (and by `onDocked` before the dock procedure ends), both of which precede the physical completion.

- Cross-session/replaced/stale evidence and nested base/override arrivals fail closed and idempotently.

## Coverage matrix

| Path | Implemented | Runtime-qualified | Notes |
|---|---|---|---|
| Same-system routes | Host adapter logic + hook | **QUALIFIED (controlled)** by `travel-in-system-station-v1` and `travel-resilience-v1` | The six in-system/station cases plus the empty-origin re-route departure boundary (`TravelInSystem` warp start) and stale-session coroutine replay were driven natively in the owner's protected runs. |
| In-system local POI arrival | `SpaceshipHasArrived` scopes | **QUALIFIED (controlled)** by `travel-in-system-station-v1` | Driven natively as `in-system-route` and `chained-route`; nested base/override scopes and stale evidence stay host-tested in `TravelArrivalScopesTests`/`TravelNativeAdapterTests`. |
| Jumpgates | Owned iterator observation | **QUALIFIED (controlled)** by `travel-cross-system-v1` | Cross-hop requested built from raw gate guids (no world lookup). Its `cross-system-jumpgate` case drove the native gate hop, with the hop's Departed/Arrived sampled inside the running native jump iterator. |
| Tutorial exit | Actual preserved; nominal raw | **SOURCE + HOST ONLY (documented limitation)** | Implemented and host-tested, never driven: `TransitionTutorialToSandbox` is called only from `JumpToSystem` on the inspected build and is a ONE-WAY world transition, and no available fixture save is still in the tutorial world. See `docs/qualification-runner.md` for the bounded proposal that would prove it (a tutorial-stage disposable fixture). Not excluded, and not claimed as native coverage. |
| Wormholes | Owned iterator observation | **QUALIFIED (controlled)** by `travel-cross-system-v1` | Nominal from waypoint target. No available fixture world contains a wormhole POI, so the opt-in `-TravelWormholeFixture` selection creates a disposable native pair with the game's own factory BEFORE the route is driven; that creation is fixture data recorded as its own non-required row, never travel evidence. Without the selection the case stays a mandatory NOT-RUN, which fails the phase. |
| Post-gate in-system continuation | TravelToNextWaypoint requests each in-system leg | **QUALIFIED (controlled)** by `travel-recovery-continuation-v1` in the owner's run qa-94 | Its `post-gate-continuation` case drives ONE native multi-waypoint route (`GenerateShortestRoute` -> `[gate, follow-on]`) across a jump gate to a safe follow-on POI in the destination system. The three legs (in-system approach, gate hop, post-gate in-system leg) carry distinct operation identities, and `RouteCompleted` is published exactly once, at the final native leg: the receipt records the native snapshot at the gate arrival, where a waypoint still remained and the jump routine still owned the transition, so withholding the completion there is observed native truth rather than a timing artefact. qa-94 drove it natively and PASSED: three legs with distinct operation identities and exactly one RouteCompleted at the final leg. A passing receipt is this phase's own case evidence; it is not owner acceptance. |
| Fast-lane gate-to-gate chain (`travelMultiplier = 7`) | Same TravelToNextWaypoint legs; no distinct API surface | **QUALIFIED (controlled)** by `travel-fast-lane-v1` in the owner's run qa-95 | The native fast lane is a DIFFERENT branch from the post-gate row above: `GamePlayer.DoFastLaneTravel()` is true only when `waypoints[0]` is a usable `JumpGate` and the save's own `fastLaneTravelUnlocked` is set, and only then does `JumpToSystem` charge the next gate (`TheGate.ChargeFastLaneTravelToNextGate`) and store `travelMultiplier = 7f`. The continuation phase deliberately ends at a safe non-gate POI, so it can never reach it. `travel-fast-lane-v1` therefore drives ONE native planner route across TWO gates into a THIRD system (`GenerateShortestRoute` -> `[gate, gate, destination]`), so the intermediate system's next waypoint is another gate. Its `fast-lane-gate-chain` case asserts five legs with distinct operation identities and exactly one RouteCompleted at the final leg; its `fast-lane-multiplier-observed` case asserts the branch actually RAN, by sampling the native multiplier at 7 with `fastLaneTravelActive` true at every boundary of the gate-to-gate leg and at 1 on the approach and final legs - on the inspected build `travelMultiplier = 7f` is stored in exactly one place in the whole assembly, immediately after the charge coroutine, which an installed-assembly test pins. The fixture's `fastLaneTravelUnlocked` flag is only READ: a locked save records an honest NOT-RUN (a phase failure) and no save, config or native flag is ever written. qa-95 PASSED both cases: five legs across three systems and two gates, one final RouteCompleted, the native multiplier at 7 on all three boundaries of the gate-to-gate leg and at 1 on the surrounding control legs. |
| Initial load placement | Ready placement observed | **QUALIFIED (controlled)** by `travel-in-system-station-v1` | Driven natively as `initial-placement`; initial placement is explicitly not travel arrival, and a placement carries no operation identity. |
| Dock/undock vs interior ready | Native Dock/Undock/EmergencyUndock boundaries; DockQuick unhooked; source-attributed docking-request intent captured at the actual assignment inside CheckForDocking | **QUALIFIED (controlled)** by `travel-resilience-v1` | Its `restore-relink-dock` case (with the mandatory `restore-reinit-same-size` subcase) was driven natively. Controlled phase `travel-resilience-v1` drives the load restore, the relink, the mandatory same-size re-init of the CURRENT ship and the different-size re-init, and requires zero physical facts from all of them while a genuine HUD dock request still emits exactly one `DockedPhysical`; Full owner acceptance is separate. Restore/relink/re-init/NPC/dungeon assignments carry no intent and never emit, independently of interior scene or instance state. One physical fact per docking request (the intent is consumed at the first successful completion; native CanDock/Docking already prevents a second coroutine, so this is a defensive guard). No ordering promise between InteriorReady and DockedPhysical: the arrival opens the interior synchronously through CheckForSpaceStationEnter, frames before the Dock() coroutine exists. A docked load whose init/relink spawn misses the docking tolerance still emits nothing, because that assignment is outside a request. |
| Direct field mutation/teleport/cheat | **EXCLUDED** | — | Explicitly excluded, not merely unimplemented: these paths mutate `GamePlayer.currentPointOfInterest`/`currentSystem` (and the cheat menu's teleport) directly, outside every native boundary the adapter observes - no request, no departure coroutine, no `SpaceshipHasArrived`, no `TravelToNextWaypoint`. There is no owned boundary at which a verified fact could be attributed, so the adapter reports nothing rather than inventing a transition. A consumer that needs to notice such a mutation must read the world itself. Re-verified against the freshly inspected `Assembly-CSharp.dll`: the direct stores of `GamePlayer.currentPointOfInterest`/`currentSystem` outside the observed travel boundaries are `Behaviour.UI.DebugScreen.ConsoleScreen.Teleport` (cheat console), `Source.Util.GamePatch.ResetDemoPosition`, the world builders (`SandboxWorld`/`TutorialWorld`/`TestArenaWorld.SetupWorld`, `Tutorial.SetupNewGame`), `GamePlayer.LoadGalaxySection` (load reconstruction, reported as placement, not travel) and `GamePlayer.TransitionTutorialToSandbox` (the tutorial rewrite row above). The observed boundaries are `TravelManager.UnloadCurrentScene`, `<Travel>`, `<JumpToSystem>` and `<TravelToWormholeDestination>`. |
| Dwell-time boundaries | Leg tracker + public `DwellSeconds` | **QUALIFIED (controlled)** by `travel-journal-comparison-v1` | Its `api-dwell-anchored` case asserts over the public facts of its own window that every reported dwell is non-negative, that at least one departure carries a strictly positive dwell EXACTLY equal to the game-time difference from its own same-session anchor (no epsilon: the adapter reads `GamePlayer.elapsedTime` separately for the tracker call and for the emitted fact, but that clock advances once per frame in `GamePlayer.Update` and all reads of one pair are synchronous inside the same frame, so the subtraction is exact and any tolerance would only hide a defect), that an unanchored departure reports none, that a departure whose own anchor is later than itself (clock rollback) reports NO dwell rather than a clamped or invented one, and that no dwell is ever computed across a session replacement. The receipt publishes anchor, departure and dwell at full round-trip precision. Legacy dwell is never compared: the archived journal anchors on its own prefix, not on the API's verified boundaries. |
| Base/override dispatch (inherited implementations) | Declaration + true-override binding | **LATENT ON THIS BUILD** (host/source only) | On the inspected build all six concrete `BasePoiManager` subclasses (`SpacestationExteriorManager`, `JumpGateManager`, `WormholeManager`, `SpaceSceneManager`, `MiningManager`, `CombatManager`) declare their own `SpaceshipHasArrived` override, re-verified against the freshly inspected assembly, so the inherited-implementation branch is **not reachable natively today**; it stays host-tested and installed-assembly-pinned rather than claimed as native coverage. |
| Recovered placement | Implemented and publicly emitted | **QUALIFIED (controlled)** by `travel-recovery-continuation-v1` in the owner's run qa-94 | Its `recovered-placement` case drives a real in-system route to its verified origin unload and then takes the player's own native cancel inside the window where the native travel routine has already assigned the destination POI, its manager reports `initializedAndReady` and the native route is STILL RUNNING (`TravelActive()`), but `SpaceshipHasArrived` has not run. The live-route requirement is load-bearing: on the inspected build the routine's own wait predicate treats a MISSING local manager as satisfied, so a route can end silently and the manager can initialize afterwards; readiness alone would then be indistinguishable from this window while nothing was travelling. The adapter's own per-frame readiness observation then publishes exactly one operation-less `RecoveredPlacement` at that POI. Nothing is injected: no adapter callback is invoked, no field is written and no production hook is disabled. Whether the window is observable at all is deliberately NOT asserted: the installed-assembly test pins the native predicate shape only, not Unity's coroutine/`CustomYieldInstruction` scheduling, and the window depends on when the destination manager's own `Init` coroutine finishes relative to the POI assignment. The bounded three attempts exist for that fixture/readiness/scheduling variability, each is persisted with its own whitelisted terminal outcome, and a MISSED attempt closes its own abandoned leg with the player's own cancel (proving the closure belongs to its own operation) and then SETTLES, inside its own window and within a bounded wait, the one `RecoveredPlacement` that closing the leg enables — otherwise that placement would land in a later attempt's quiet window and fail it through the harness's own doing. An unproven closure or an unsettled cleanup ends the case with a NOT-RUN instead of retrying; the settled placement is never counted as coverage. qa-94 acquired that live window on its FIRST attempt and published the positive. The miss-cleanup paths (abandoned-leg closure and its bounded settlement) were therefore NOT exercised natively and remain host-tested only. An unobserved window is a mandatory NOT-RUN (a phase failure), never coverage, and a window wait that expires while the native route is still running fails the case at the timeout instead of retrying — with the residual recorded honestly, because the ordinary cancel does not reset the vanilla `isWarping` flag and a failed phase leaves no world a later phase may continue from. `early-cancel` keeps asserting its ABSENCE. |
| Tutorial exit rewrite | Implemented; actual preserved, nominal raw | **SOURCE + HOST ONLY (documented limitation, not an exclusion)** | The same cell as the `Tutorial exit` row above, from the API side: the requested destination stays the gate's RAW `targetSystemGuid`/`targetPoiGuid` while the ARRIVED fact reports the rewritten actual destination. On the freshly inspected `Assembly-CSharp.dll` the rewrite (`GamePlayer.TransitionTutorialToSandbox`) is reached from exactly one call site, `TravelManager.<JumpToSystem>d__103.MoveNext`, and it is a one-way world transition. It is therefore host-tested and source-attested, never driven, and never claimed as a driven positive. It is NOT excluded: the bounded, safe proposal that would prove it natively (an opt-in phase on a DISPOSABLE tutorial-stage fixture save) is recorded in `docs/qualification-runner.md`; no such fixture exists today. |

The separate optional phase `travel-resilience-v1` drives the three remaining reducer boundaries natively: the empty-origin re-route departure boundary (`TravelInSystem` warp start), the restore/relink/re-init `Dock()` suppression path (both docking-size branches: the mandatory same-size re-init of the current owned ship and the different-size branch that takes a real `Dock()` coroutine, with a genuine docking request in the same window as a positive control) and stale-session replay of captured old `Undock()` coroutines after a replacement load. Its receipt is evidence for its own three case identities only and never widens the other two phases, whose optional rows for those cells stay NOT-RUN.

The separate optional probe `anima-travel-consumer-v1` adds the first of the two active consumer integrations as controlled evidence: it REUSES the two travel phases in place (it must observe them before the Anima mission pilot's `StopProvider` disposes the consumer's observer) and compares the installed Anima 0.4.0 consumer's own visited-system registry, its v4 sidecar, its session/leg latch and its documented visit-only degraded state against the witnessed public facts. It drives no travel of its own and adds no travel coverage.

The separate optional probe `echo-travel-consumer-v1` adds the second active consumer integration the same way (Echo's final-route arrival snap; qualified in qa-91 with the API present and qa-92 with the API absent). Anima's system-visit recorder was qualified in qa-86.

The separate optional phase `travel-journal-comparison-v1` adds the sandbox-only comparison against the UNCHANGED archived TravelJournal, and carries the native dwell assertions. It reads only the sidecar files the archived plugin wrote itself in response to real vanilla saves; it never edits, rebuilds, reactivates, migrates or bridges the archive, never calls its API surface or reflects into its store, and never treats its history as ground truth.

The separate optional phase `travel-recovery-continuation-v1` adds the two cells the other phases deliberately left open: the positively driven `RecoveredPlacement` and the post-gate in-system chain continuation. Both PASSED natively in the owner's run qa-94, on the recovery case's first attempt, so its miss-cleanup paths stayed host-tested only. It does NOT reach the native fast lane (gate-to-gate, `travelMultiplier = 7`), which is why the separate optional phase `travel-fast-lane-v1` exists: it drives a two-gate planner route into a third system and proves the native charge branch ran by the transient multiplier it sets. That phase PASSED natively in the owner's run qa-95: five legs, three systems, two gates, the native multiplier at 7 on all three boundaries of the gate-to-gate leg and at 1 on the approach and final control legs, and exactly one final RouteCompleted. It drives its own routes with the same vanilla entry points (`CanWeTravel`/`TryInitiateTravel`/`CancelTravel` and the station exit), needs only the in-system phase's travel capability, and reserves its own process budget.

Remaining after those phases, and explicitly NOT covered by controlled native evidence: the tutorial exit rewrite (source + host only, with a bounded proposal recorded in `docs/qualification-runner.md`), the inherited base/override dispatch branch (latent: all six concrete managers declare their own override on the inspected build), the excluded direct field mutation/teleport/cheat paths, the recovery phase's miss-cleanup fallbacks (host-tested only, because qa-94 acquired its live window on the first attempt) and FULL owner acceptance. A passing phase receipt is evidence for that phase's own case identities only, and no probe here is owner-scoped content persistence: automatic owner-managed content stays with #13/#14 (M04) and the owner-scoped world identities travel destinations must resolve stay with #21 (M06).

TravelJournal remains archived and must not be edited, rebuilt, reactivated, migrated or bridged. BOTH active consumer integrations are delivered and driven natively: Anima's system-visit recorder (`anima-travel-consumer-v1`, qa-86) and Echo's final-route arrival-snap (`echo-travel-consumer-v1`, qa-91 with the API present and qa-92 as the API-absent control). Echo's distinct ETA-sync remains a separate integration and is NOT claimed here. The archived recorder is only a read-only, sandbox-only comparison history owner (`travel-journal-comparison-v1`, qa-93): it is never bridged, never migrated and never treated as ground truth, and it is not a third consumer integration.

## Controlled native evidence matrix

Each phase is evidence for its OWN case identities only. The receipts themselves stay private
(sandbox artefacts, private saves and world identities are never published); the aggregate case
identities, statuses and counts below were read from those receipts. Every run listed here recorded
`PASS`, an unchanged protected save set and a self-terminated game process.

| Phase / probe | Mandatory case identities | Owner run | Receipt outcome |
|---|---|---|---|
| `travel-in-system-station-v1` | `initial-placement`, `station-undock`, `in-system-route`, `early-cancel`, `chained-route`, `station-dock` | qa-79 (also re-run in qa-81/84/86/91/93/94/95) | 11 rows: 6 passed, 5 optional NOT-RUN (the other phases' cells), 0 failed |
| `travel-cross-system-v1` | `cross-system-jumpgate`, `cross-system-wormhole` (opt-in disposable wormhole fixture) | qa-81 (also re-run in qa-86/91/93) | 3 rows: 3 passed (both cases plus the non-required `wormhole-fixture-setup`), 0 failed |
| `travel-resilience-v1` | `empty-origin-reroute`, `restore-relink-dock` (+ mandatory `restore-reinit-same-size`), `stale-session-replay` | qa-84 | 4 rows: 4 passed, 0 failed |
| `anima-travel-consumer-v1` | `consumer-binding`, `non-travel-quiet`, `gate-arrival-visit`, `wormhole-arrival-visit`, `visit-persistence`, `recording-degraded` (+ 3 mandatory subcases) | qa-86 | 9 rows: 9 passed, 0 failed |
| `echo-travel-consumer-v1` | `arrival-snap-binding`, `no-snap-quiet`, `in-system-final-snap`, `gate-final-snap`, `wormhole-final-snap`, `earlier-subscriber-supersession`, `snap-stop-degradation` (+ `declared-probe-controls`) | qa-91 (API present), qa-92 (API-absent control) | 8 rows: 8 passed, 0 failed; the control run recorded the consumer loading and its native timing hook firing with the API absent |
| `travel-journal-comparison-v1` | `legacy-binding`, `in-system-arrival-compatible`, `chained-arrival-compatible`, `jumpgate-prefix-lead`, `wormhole-transit-gap`, `station-interior-vs-physical`, `legacy-blind-concepts`, `journal-io-containment`, `api-dwell-anchored` | qa-93 | 9 rows: 9 passed, 0 failed; the post-quit audit recorded every archived-journal file inside the sandbox saves and the prepared binary hash unchanged |
| `travel-recovery-continuation-v1` | `recovered-placement`, `post-gate-continuation` | qa-94 | 3 rows: 2 passed, 0 failed, plus one NOT-RUN diagnostic attempt row — the single attempt reports `cancelled-in-live-window`, so the miss-cleanup fallbacks never ran |
| `travel-fast-lane-v1` | `fast-lane-gate-chain`, `fast-lane-multiplier-observed` | qa-95 | 2 rows: 2 passed, 0 failed; five legs / three systems / two gates / one final completion, and the native multiplier recorded at 7 on all three boundaries of the gate-to-gate leg with 1 on both control legs |

Host and installed-assembly evidence backs every phase: the pure receipt rules, the reducer and the
adapter are host regressions, and the native members and call structures each phase depends on are
pinned against the installed `Assembly-CSharp.dll` by the `InstalledGame` suite.

## Evidence contract

An accepted request is not departure; iterator creation is not arrival. Each leg has a session-scoped operation ID and retains its requested destination. Completion requires separate observed departure and destination readiness evidence; the actual destination may differ (notably tutorial exit). Repeated departure/arrival evidence is idempotent; the adapter must coalesce nested request callbacks before creating a leg. Superseding a pending leg records cancellation before the new request; old leg/session evidence cannot affect its replacement. Session reset discards queued old evidence.

Initial placement is explicitly not travel arrival. A verified departure ends a known dwell interval and verified arrival begins another. Clock rollback produces unknown dwell, not a negative duration. Cancellation after departure does not pretend the ship returned to its origin. `RecoverPlacement` accepts readiness-backed placement after an interrupted, previously placed leg, only with no pending leg and unknown current location. It begins a new dwell interval without a request/departure/arrival fiction. It cannot replace a known location or complete a still-pending leg. Initial readiness received during an accepted request is deliberately not adopted: origin/dwell remain unknown until an attributable boundary; cancelling before departure permits initial placement again.

Internal fact `Location` means requested destination for Requested, observed location for Departed/Cancelled, and actual location for placement/arrival. The prepared public `TravelTransition` instead exposes `Origin`, `RequestedDestination` and `ActualLocation`, session/operation identity, mode, sequence and game-time/dwell fields. Placements have no operation ID; first verified placement can occur after an interrupted initially unplaced leg and is not a session-start notification. Stale-session/leg evidence is ignored before validating its unused fields.

The internal event hub enforces main-thread access, isolated subscribers/diagnostics, queued reentrancy, disposal and replacement-session rejection. `CurrentLocation == null` means unknown or in transit, not proof of departure. `RouteCompleted` is emitted from the verified `TravelToNextWaypoint` boundary and only for final-route completion, not a request or every intermediate POI arrival. A main-thread violation faults the travel group (capability/service disabled); a stale/replaced-session operation error is reported but never disables the replacement.

The `SpaceshipHasArrived` binding set covers in-system arrivals only: jumpgate and wormhole routines never call it and need their own iterator/readiness observations. Binding helpers enumerate concrete method declarations on the abstract POI base and true overrides in the inspected game assembly, base-first to prevent JIT inlining gaps. Inherited implementations reuse their declaration; hidden non-overrides are not treated as overrides. Whole-assembly type-load failure deliberately aborts resolution rather than using a partial type set; installation must catch it and disable the entire travel group, not break plugin startup. Scope coalescing prevents duplicate nested/reentrant callbacks for one manager without dropping independent managers; caught nested failures cannot manufacture success. The adapter must still verify live Unity objects, readiness, player/session/leg identity and native outcomes before publishing.

Location reads use stored `MapElement._name`, not its lazy name getter, so observation cannot generate names or consume world randomness. Both names are nullable when unavailable; an actual empty string remains distinct from unavailable. Readiness requires the exact current POI reference, not merely some initialized manager; mixed system/POI snapshots are rejected.

Location keys in the reducer are opaque native system/POI identifiers (null POI means empty space), not names or provider-local creation IDs. #21 must resolve future API-created destinations through stable owner-scoped restored world identities. Observations require no content serializer; optional journal history remains owned by the journal.

## Source coverage matrix

Fresh inspection uses original assembly SHA-256 `a2aad60bc68c31baccd636587d3c5ba4e651eacda59b0af42cd4f17f864284fb`. This is a source map and pending delivery checklist, not a supported-path declaration.

| Path | Source boundary / hazard | Remaining implementation and evidence |
|---|---|---|
| Same-system routes | `TryInitiateTravel` can refuse; `SetRouteToPOI` sets a target synchronously. `StartTravel` can wait for undocking/extraction and preparation before `Travel`. | Capture accepted requests separately from actual departure; cancellation and in-transit load tests. |
| Local POI arrival | `Travel` assigns current POI before waiting for local readiness and calling virtual `SpaceshipHasArrived`; it can immediately start another waypoint. | Capture before chain replacement; cover every declared override plus inherited base implementations without nested duplicates. |
| Jumpgates | `JumpToSystem` yields before assigning current system/POI and requesting the destination scene. | Wrap iterator advancement with session/leg ownership; qualify actual system transition and local readiness, never factory return. |
| Tutorial exit | Jump routine can replace its nominal destination through `TransitionTutorialToSandbox`. | Preserve request separately, report actual replacement system/POI. |
| Wormholes | `JumpToWormhole` yields `TravelToWormholeDestination`, which updates location and waits for its manager. | Observe nested advancement and readiness without flattening Unity yields. |
| Fast-lane chains | Jump routine may charge the next gate and call `TravelToNextWaypoint`. | Separate leg identities; observe intermediate arrivals before starting the next leg. Delivered as `travel-fast-lane-v1` (two-gate planner route, transient `travelMultiplier = 7` sampled at the gate-to-gate leg's own boundaries); both required cases passed in controlled qa-95. |
| Initial load | Reconstructed location is present before local readiness. | Emit placement, not a fabricated transit; test transit and empty-space loads. |
| Dock/undock and interior | `SceneLoader.ToggleSpaceStationInterior` starts asynchronous loading/unloading; its return does not prove interior readiness. | Inspect docking/interior owners and scoped lifetimes separately; no universal GameplayInitialized shortcut. |
| Teleport/cheat/direct field mutation | INSPECTED (see the coverage matrix): the direct `currentPointOfInterest`/`currentSystem` writers outside the observed boundaries are the cheat console teleport, the demo/save patch, the world builders, the tutorial setup, load reconstruction and the tutorial rewrite. | Decision taken: EXCLUDED, with the writer list as the rationale; load reconstruction is reported as placement, not travel. |

TravelJournal remains archived and must not be edited, rebuilt, reactivated, migrated or bridged. The two active integration targets are Anima's system-visit recorder and Echo's final-route arrival-snap; Echo's distinct ETA-sync remains separate. The unchanged archived DLL is additionally a sandbox-only comparison history owner, not ground truth for its known prefix/override/timing gaps. A test-only recorder is not a substitute for either active consumer. Native API hook qualification and both consumer integrations were merge gates for #12 and are now delivered as controlled native evidence (qa-86, qa-91/qa-92); they were never gates satisfied by this reducer alone.
